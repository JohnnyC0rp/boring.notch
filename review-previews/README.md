# Component previews

These images render the feature components with synthetic data. They contain no captured clipboard entries, private calendar events, transcript content or recorded audio. Calendar clocks are fixed for boundary and Today-feedback examples; audio levels are supplied test values.

This branch hosts review images for the independent code proposals. It is not part of the application build.
